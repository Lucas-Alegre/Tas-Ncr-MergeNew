ssfw = {

    "base" : include(cim/base_cim.json),

    "type" : function() {
        States.getProperty("checkStatus")()
        .then(
            function(ok){},
            function(error){} 
        )
        .caugth(function(e){});
    },

    "events": {
        //"cim_available" : "", Esta salida se maneja desde base_cim
        //"cim_unavailable" : ""   Esta salida se maneja desde base_cim
    }
}